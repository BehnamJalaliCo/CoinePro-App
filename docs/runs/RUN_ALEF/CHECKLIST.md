# RUN א — checklist

States: ✅ done · ❌ not done, or narrowed (the row says exactly how) · ⏳ owed to the owner.

The Greek runs are finished at Ψ and Ω is taken, so this one is named for the first letter of the
Persian alphabet. Fitting, as it happens: it is the run where the product stops waiting for an
American store and starts distributing itself.

The owner's brief, in their own words, was one sentence with two halves:

> «کپی ترید البنک میماند و در ضمن گوگل پلی به ما ایرانیا خدمات نمیده در حال ایمیل زدن و مکاتبه
> هستیم باهاشون»

The first half closes a question left open at the end of run Ψ. The second half is a fact about the
world, not an instruction — and it turned out to have a great deal of work hiding inside it.

---

## א1 — the answer to `RUN_PSI/BLOCKED.md §Ψ16`

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 1 | LBank copy trading: gone, or the web panel's? | ✅ **answered: it stays** | `RUN_PSI/BLOCKED.md §Ψ16` carries the answer and is closed; `RUN_PSI/CHECKLIST.md` row 16 is ✅ and `RESUME.md`'s first open question is struck through. The second of the two branches that section named, so the work it would have triggered — three passages out of the terms, a re-sync, a note off the membership gate — does not happen | — **a decision whose evidence is that the diff is empty.** `sync-legal-documents.py --check` passes on the unchanged documents |
| 2 | Saying *why* the app carries none and the terms describe some | ✅ | Written down rather than left for the next reader to wonder at: **the terms are the service's, not this binary's.** They are the same document the web panel serves, a reader who agrees to them really can have the feature, and a document describing only what one client happens to draw would be the wrong document | — **prose.** `RUN_PSI/BLOCKED.md §Ψ16`, «The answer» |

---

## א2 — no Google Play, and the hole that leaves

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 3 | What the fact actually costs, written down once | ✅ | `docs/release/DISTRIBUTION.md` — the two separate refusals (no developer account from Iran; no installs served into it), how a reader really gets the app, **the four things a store would do that the app now has to do for itself**, the update document's contract, and which `assetlinks.json` fingerprint is the right one | — **a document.** It is the deliverable |
| 4 | The app had **no** update mechanism at all | ✅ **built** | `grep -rn "AppUpdate\|updateAvailable\|latestVersion\|InAppUpdate" app/src/main core feature` returned nothing before this run. With no store and no check, an old build was silently permanent: a fix shipped today reached only whoever happened to read the channel it was announced on. `:core:update` is the answer | — **an absence, now filled.** The module and its test |
| 5 | The comparison, and the four ways a published release is refused | ✅ | `AppUpdate.decide` compares **`versionCode` only** — the integer the package manager itself orders installs by. `publishable()` refuses a non-HTTPS address, an address off `PUBLISHING_HOSTS`, a missing or malformed digest, and a record the host has not finished writing. Each refusal is `Unknown`, which draws nothing | — **a decision function, and a still cannot state one.** `AppUpdateTest`, thirteen cases, including `https://pro-chart.com@evil.example/…` |
| 6 | The card on «ایمنی و انتشار» | ✅ | Version, what changed in the reader's own language, the file's SHA-256 before they fetch it, and one button. Silent in both non-states, which is the behaviour that lets it live on a screen every reader visits | `alef-update-available-fa`, `alef-update-mandatory-fa`, `alef-update-available-en-light` |
| 7 | Silent when there is nothing to say | ✅ | The state a frame cannot photograph, so it is asserted twice — `Current` and `Unknown` — each against a render that first proves it is the right screen, or «no card» would be satisfied by an empty one | — **an absence.** `AlefProofTest`, three cases, each asserting «ایمنی و انتشار» is present before asserting the card is not |
| 8 | The app does not download and does not install | ✅ **deliberately narrowed** | `UpdateHandoff` hands the address to the browser. In-process installing means `REQUEST_INSTALL_PACKAGES` — permission to put *any* package on the phone — plus a download manager, a file provider and a directory of half-fetched APKs, bought to save three taps the reader has already performed once. The permission is not in the manifest and is not wanted | — **an absence.** No `REQUEST_INSTALL_PACKAGES` in `AndroidManifest.xml` |
| 9 | `mandatory` changes a sentence and never a door | ✅ | A flag on a host that could stop an installed app from opening is a remote kill switch, and a kill switch is one compromised host away from being everybody's app at once. The word is drawn in the warning colour and the screen is otherwise identical | `alef-update-mandatory-fa`, and `AppUpdateTest` asserts the decision is unchanged by the flag |
| 10 | The check carries no credential | ✅ | Its own OkHttp client, built with no `bearerToken`. Both existing clients attach a session token to everything they carry, and the brand host is neither platform's — a call on either would hand that platform's bearer token to a machine with no business holding one. Unpinned too, because `pro-chart.com`'s certificate has never been seen from here | — **a client configuration.** `AppModule.appUpdateGateway`, and its note |
| 11 | Asked once per visit to the screen, not at every launch | ✅ | A reader who has opened «ایمنی و انتشار» is already asking the app about itself, which is the one moment «there is a newer one» is an answer rather than an interruption. A launch-time check would be a request on every cold start, on networks where a request costs something, for a fact that changes a few times a year | — **a `LaunchedEffect` keyed on the gateway.** The route in `CoineProApp.kt` |
| 12 | Strings in both locales, inside the note policy | ✅ | Nine keys in `values/` and `values-fa/`. `update_manual_body` and `safety_signing_body` are `visible` in `notes.tsv` and in `NotePolicy.visible` — drawn inline rather than folded into an ⓘ, because «you fetch the file and install it yourself» is an instruction and an ⓘ is where an instruction goes to be missed | — **text.** `lint_strings.py`: 41 modules clean |

---

## א3 — `assetlinks.json`, which the same fact makes simpler

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 13 | Which fingerprint the file must name | ✅ **corrected** | It was «the App Signing key, from Play Console». With no Play in the path nothing re-signs the upload, so it is the **release keystore's own** SHA-256. `print-assetlinks.sh`'s header, `SERVER.md §8.4`, `SERVER_BUILD_PROMPT.md` Phase 1 and `DOMAINS.md` step 3 all said the old thing and all now say this one, with the reason rather than the instruction | — **comments and documents.** Four files |
| 14 | The owner can read it off their own phone | ✅ **the dangling KDoc is closed** | `LaunchReadinessScreen` carried a parameter's documentation — «the certificate this install is actually signed with» — with **no parameter under it**. The parameter now exists: `signingFingerprints`, SHA-1 and SHA-256, monospace, left to right, with a copy button. `AppIntegrity.fingerprints` was already there and nothing on this screen called it | `alef-signing-certificate-fa` |
| 15 | A build that cannot read its own certificate | ✅ | Draws no card at all rather than an empty one. `AppIntegrity.fingerprints` answers an empty list on any failure and the screen takes that as «nothing to say» | — **an absence.** `AlefProofTest`, the empty-list case |

---

## א4 — what the server has to serve, now that it is the update channel

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 16 | The contract for `GET /api/app/latest` | ✅ | `SERVER.md §4.6`, new: the two routes, the document field for field, and the four things the app enforces — with the consequence stated plainly, that a release failing any of them is **silently not offered to anybody**. It is the one route on that server that is not a relay, because nothing upstream knows what the Android release is | — **a specification** |
| 17 | The brief the server's agent works from | ✅ | `SERVER_BUILD_PROMPT.md` gains **Phase 1½**, between the legal pages and the relay, with its own acceptance commands. Placed there on an argument rather than by taste: it is the only step whose absence gets *worse* with time, because every release that ships without it is another cohort with no way to hear about the next one | — **a brief, and the acceptance commands in it are the test.** Phase 1½ ends with three `curl`s and the answers they must give |
| 18 | Where it sits in the go-live order | ✅ | `SERVER.md §7` step 2½ and `DOMAINS.md` step 4, both with the proof that closes them — the served `version_code` against `version.py --code` for the published tag | — **an order of work** |
| 19 | The server stands it up | ✅ **built, and verified from this side** | Phase 1½ is served. Every claim in the server's acceptance report was re-checked here against the published artefacts rather than trusted: `aapt2` says `versionCode='50000004'`, `sha256sum` of the release APK is the `f6f9925…738c2` the document names, and `AppUpdate.publishable` holds on all four rules. **Not yet reachable** — 200 on the origin, 526 through Cloudflare (§א21) | — **a document and a file, and the evidence is that four independent readings agree.** `BLOCKED.md §א19`, the table |

---

## א5 — the two Play documents, which are now preparation rather than description

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 20 | `docs/PLAY_LISTING.md` | ✅ **kept, and re-framed** | A note at the head says the product is not on Play and points at `DISTRIBUTION.md` for how it actually ships. Kept rather than deleted because the owner is corresponding with Google and the day that succeeds this is the day's work already done — but **nothing in the build may assume any of it** | — **a document's own framing, which no frame can photograph.** The note is the first thing in the file |
| 21 | `docs/PLAY_COUNTRIES.md` | ✅ | Already said Iran is not a country a Play Console account may be registered from, and that the real route is a direct APK. It gains the one sentence it was missing: that the app now carries the update channel itself, because there is no store to carry one | — **a paragraph in a document, not a screen.** The note above «اول، چیزی که همه‌چیز را عوض می‌کند» |

---

## א6 — executed in full

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 22 | The new surface rendered at a tablet width **on the day it was written** | ✅ | `RUN_PSI/RESUME.md`'s second instruction to the next session, followed rather than read: two of the last three full-screen surfaces were wrong on a tablet, and both were caught a run late. The card has a pixel-tablet frame in the same commit as the card | `alef-update-available-pixel-tablet-fa` |
| 23 | Every gate, the whole suite, a signed build | ✅ | Nine gates green; the full unit suite green; shipped as 5.0.0 | — **the commit and the release** |
| 24 | Why 5.0.0 and not 4.100.0 | ✅ | The versioning scheme reserves two digits for MINOR — `versionCode = MAJOR×10,000,000 + MINOR×100,000 + …` — so 4.99.0 is the last of the fours and `version.py --bump minor` refuses to go further. It is the scheme choosing, not taste; that it lands on the release where the product leaves the store behind is a coincidence and a convenient one | — **arithmetic.** `scripts/release/version.py`, `MAX_MINOR = 99` |

---

## א7 — Android CI, red for six commits before this run touched it

Not in the brief, and found because 5.0.0's push was watched rather than assumed. Three jobs were
failing and none of the three failures was new; the owner's own gate list is green throughout, which
is exactly how a workflow stays red without anybody noticing.

**Android CI is green on `0f33dbb`** — the first time in at least six commits.

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 25 | `:benchmark` had not compiled for weeks | ✅ **fixed** | A KDoc in `ChartFlingBenchmark` documented an output path ending `…/*.json`. **Kotlin block comments nest** — unlike Java's — so the slash-star inside the comment opened a second one, the closing delimiter shut only that, and the remaining two hundred lines of the file were comment. The error read «Unclosed comment» against the last line of the file, which is about as far from the cause as a diagnostic gets. The path is written without a glob now, and the note beside it says why | — **a compiler error, and the evidence is its absence.** `:benchmark:compileBenchmarkKotlin` green, built without `--offline` so the macrobenchmark artifact could be fetched |
| 26 | `:app:lintDebug` failed on two proof tests | ✅ **fixed** | `StateFlowValueCalledInComposition`: `controller.state.value.signals` read *inside* a `proof { }` composable in `RunOmegaFixProofTest` and `RunOmegaProofTest`. Hoisted above the lambda in all three places. The rule is right even in a test — a frame whose state cannot recompose is a picture of the wrong moment, which is the same fault run Ψ found in the welcome captures | — **a lint rule.** `:app:lintDebug` green here, 0 errors against 41 warnings |
| 27 | The on-device proof test died before it drew anything | ✅ **fixed, unverified** | `DeviceProofTest` swaps the locale with `createConfigurationContext`, which builds a context from the **base** rather than from the activity — so the wrapper chain no longer reaches a `ComponentActivity`, and `rememberLauncherForActivityResult` could not find its owner. Any scene with a picker in it threw. `LocalActivityResultRegistryOwner` is now provided from the rule's own activity | — **an emulator test; this container has none.** It compiles (`:app:compileDebugAndroidTestKotlin`), and CI's emulator is the verdict |
| 28 | And the fault the comment had been hiding | ✅ **fixed** | Once `:benchmark` parsed again it stopped on `device.performMultiPointerGesture` — a method `UiDevice` does not have. It is on `UiObject`, the `UiSelector` API, and on nothing else: not on `UiDevice`, which carries only the single-pointer `swipe`, and not on the `UiObject2` the neighbouring `pinch()` gets from `findObject(By…)`. **A file that does not parse hides every error after the first**, and this one had been hidden for as long as the comment was open | — **a compiler error.** Same task, same run, green |

---

## א8 — the server came back, and four of `SERVER.md`'s rows were wrong

The Pro Chart machine finished Phase 0 and Phase 1 and reported four disagreements between the spec
and the live backends. All four were real. The spec was written from **what the Android app calls**,
and the Android app signs in — so every route in it was the authenticated surface, and a relay with
no account reaches none of them.

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 29 | The missing distinction: each backend has **two** surfaces | ✅ **new `SERVER.md` §4.0** | `api/mobile/v1/*` and `user/*` are authenticated and answer 401 to a relay with no account, which is correct rather than broken. Beside them sit `api/v1/public/*` (TradeYar) and `api/public/*` (CoinePro-FX). **The web's Phase 2 is the guest tier**, and the guest tier is already specified, shipped and proved on the phone — `:core:guest` adapts those exact routes to the same two interfaces the signed-in chart uses. So Phase 2 does **not** wait on `PLAN.md` §6.2; that question gates the account | — **a specification, and the proof is that the app already ships a client for it.** `GuestApi`, `GuestMarketGateways.kt` |
| 30 | §4.1's FX candle row named a TradeYar route | ✅ **corrected** | `api/v1/public/candles/{symbol}` is TradeYar's, copied into the forex column. The app's own FX candles come from `academy/chart/{symbol}` behind a second token, which no Phase-2 relay can hold. The public route is `api/public/prices/series` — **four timeframes** (`M15 H1 H4 D1`; `M5`, `M30`, `W1` are a `422`, all seven measured), `limit` bounded `20..400`, `t` an ISO-8601 string and no volume. That is the academy route minus the five-minute bar | — **a route table.** `SERVER.md` §4.1 and the probe log in §4.7 |
| 31 | The bare snapshot is not the FX discovery mechanism | ✅ **corrected, and it found a product fault** | Rule 6 said both backends answer the bare call with everything. TradeYar does — 857. **CoinePro-FX answers 17 and omits gold and silver**, while `api/public/prices/live` answers 19 with both. Verified here independently of the server | — **two numbers.** `SERVER.md` §4.7, and §א20 |
| 32 | §4.3 was mostly Phase 4 pretending to be Phase 2 | ✅ **split** | `public/signals/*` sits under a path called *public* on CoinePro-FX and is still behind VIP — `EndpointCatalog` says so in as many words and the live server agrees. The calendar and both market-intelligence routes likewise. What Phase 2 **can** carry is now its own table: `api/v1/news/list`, `api/demo/signals` (closed signals with their real outcome — a track record, not a demonstration), `api/v1/public/community`, `api/v1/public/membership` | — **a route table.** `SERVER.md` §4.3 |
| 33 | Gold is missing from the phone's forex list too | ⏳ **owed to the owner — one line to CoinePro-FX** | `MarketCatalogGateway` builds the forex catalogue from `ws/snapshot` and nothing else, on purpose. So the metals are absent from the app's forex market list today, while `ForexSignalScope` narrows the forex **signals** to gold: a reader can be shown a gold call and find no gold market to open. Nothing in the app may fix it — inventing a market the feed does not list is the fault the catalogue exists to prevent. The KDoc that claimed both backends answer alike now records the measurement instead | — **a backend's answer.** `BLOCKED.md §א20` carries the ask verbatim |
| 34 | The fingerprint question, asked again | ✅ **made unmissable** | The server asked for the Play Console App Signing key. It had already been corrected in Phase 1; it is now **rule 7** at the top of the brief as well, where a reader meets it before the phases. There is no Play in this product's path, nothing re-signs the upload, and the value is the release keystore's own — which the app prints on its own «ایمنی و انتشار» screen | — **a rule.** `SERVER_BUILD_PROMPT.md`, rules 7 and Phase 1 step 3 |
| 35 | `pro-chart.com` answers 526 | ✅ **named, with the refusals spelled out** | Cloudflare reached the origin and refused its certificate. Let's Encrypt cannot complete a challenge through an orange-clouded record, so the answer is a Cloudflare **Origin CA** certificate from the owner's dashboard. The step says so, and says plainly that turning off verification, serving plain HTTP to the origin or switching the zone to Flexible all make the 526 disappear while leaving the hop unencrypted | — **a configuration step.** `SERVER_BUILD_PROMPT.md` Phase 1 step 5 |
| 36 | The fingerprint, confirmed without a phone | ✅ | The server read it from the published APK's **v2 signing block** — the APK carries no v1 signature at all, which is why `keytool -printcert -jarfile` answered nothing — and checked three releases. Confirmed here independently with `apksigner verify --print-certs` on a *fourth*: `9612ab6c…fbd0`, `CN=CoinePro, OU=Mobile, O=CoinePro, L=Tehran, C=IR`. The value is written down in `DISTRIBUTION.md` §5, because a fingerprint nobody has recorded is one nobody can notice changing | — **a hex string, and four readings of it that agree.** `apksigner` output, quoted in `BLOCKED.md §א19` |
| 37 | `releases/latest` had been dead for thirty releases | ✅ **fixed** | The workflow hard-coded `prerelease: true`, so GitHub had no latest release to point at and `releases/latest` answered `302` to the *list*. The website's «download the app» button was aimed at exactly that. It is `${{ steps.ver.outputs.build != '0' }}` now: the named version is a release, the `-bN` builds on top of it are pre-releases — which is what semver means by the `-` as well as what GitHub means. Nothing in the app noticed because the update document names an exact URL, and that is how it went unseen | — **a workflow input.** `.github/workflows/android-apk.yml`, and the note above it |
| 38 | The card's own sentence had no author | ✅ | `notes_fa` / `notes_en` are the one place this product speaks to a reader about itself, and they were being written by whoever was publishing, from a boilerplate release body and a `CHANGELOG.md` that stopped at 0.2.0. `docs/release/UPDATE_NOTES.md` is the source now — per version, both languages, with the rule that anything which cannot be said in both is said in neither | — **text.** The file, and 5.0.0's entry in it |
| 39 | The notes file became an interface, so it got a gate | ✅ | The server does not retype those sentences — `app-latest.py` **parses** `UPDATE_NOTES.md`, keyed on the released version (`5.0.0+4` reads `## 5.0.0`), unwraps the blockquote and copies the result byte for byte. That turns a document into a contract, and a contract nobody checks breaks silently **on a screen most readers open twice a year**. `scripts/release/check-update-notes.py` parses it the same way and fails the build on a missing entry, a missing language, Persian that breaks the house orthography, or either language past 400 characters. In both workflows | — **a parser, and a still cannot run one.** The gate prints what a reader will see; the CI log is the evidence |
| 40 | Its reading and mine agree byte for byte | ✅ | The server reported `notes_fa` 244 characters and `notes_en` 274. Reproducing its rule here gives **244 and 274** — the same numbers from a parser written without sight of theirs, which is the only kind of agreement worth having between two programs that have to read one file | — **two integers.** The gate's own output |

---

## א9 — the host answers, and the oldest loose end in the product is closed

The owner made the Origin CA certificate; the server installed it without disabling anything;
`pro-chart.com` is live and Phases 1, 1½ and 2 are all serving. Everything below was measured from
outside, through Cloudflare, as a reader's phone would — not read from the server's report.

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 41 | Every legal link in the shipping app now opens a page | ✅ | `/legal/terms/`, `/legal/privacy/`, `/legal/delete-account/` → `200`, **zero redirects**, `text/html`. Those three addresses have been compiled into the app since 4.47.0 and answered nothing until today; `DOMAINS.md` carried the sentence «a legal link from inside the app opens a browser on a host that does not answer» for a year. It does not any more | — **three status codes.** `SERVER.md` §4.8 |
| 42 | `assetlinks.json`, and the key in it | ✅ | `200`, `application/json`, no redirect — the three things Android's verifier asks. The fingerprint it serves is `96:12:AB:…:FB:D0`, which is the release keystore's, checked here against `apksigner`'s own reading of the APK rather than against the server's report of it. The App Link on `/reset` can verify now | — **a hex string compared to a hex string.** §4.8 |
| 43 | The update card is live for every install below 50000004 | ✅ | `/api/app/latest` answers `200 application/json`; the notes are **byte-identical** to `UPDATE_NOTES.md`; the APK it names answers `200` and `sha256sum` makes it **byte-identical to the GitHub release**. The whole chain — document, digest, file, signature — holds end to end from outside | — **four artefacts that agree.** `BLOCKED.md §א19` and §4.8 |
| 44 | The relay, and the thing it refuses to do | ✅ | 857 crypto rows and 19 forex rows with gold first — the same two numbers §4.7 measured against the backends directly, so nothing was lost in the hop. And **it does not translate a refusal**: `M5`, `M30` and `W1` come back as the backend's own `422` rather than as the nearest timeframe the relay could serve. A substituted timeframe would draw a chart of the wrong bars with nothing anywhere saying so | — **seven status codes and two counts.** §4.8 |
| 45 | The candle-cache decision, which went against the spec and was right | ✅ **spec amended to match** | §4.1 said to key each bar `venue:symbol:interval:openTime`. Rebuilding a response from individually cached bars means the relay writes `server_time_ms` itself — and **rule 1 is that this server is never the author of any number**. Caching the body whole with a TTL that runs to the bar's close gets §4.1's effect (657 ms cold, **3.5 ms** warm) without the relay acquiring a second contract. §4.9 records it | — **a cache policy, and two timings** |
| 46 | A health check that reported a fault which was not there | ✅ | TradeYar's `/healthz` answers `307` to `/login?from=/healthz`; the relay does not follow redirects, so the first reading was a false «degraded». Probe moved to `api/v1/system/health`, which answers `200` directly. Recorded because that failure mode is the dangerous one — a check that cries wolf teaches everybody to ignore it | — **two status codes.** §4.9 |
| 47 | The showcase route, read off the OpenAPI rather than taken on my word | ✅ | §4.3 said «read it off `/api/openapi.json` rather than taking this paragraph's word for it», and that is what was done: `public/signals/showcase` exists, answers without a token, and is relayed at `/api/fx/showcase`. It returns `{"signal": null, "live": null}` today, which is **empty rather than broken** — `public/signals/stats` beside it says `total_signals: 0` | — **a route that now exists in the table.** `SERVER.md` §4.3 |
| 48 | A third witness for the missing gold | ✅ | That same `stats` route reports **`symbols_covered: 19`**. The desk's own count of its forex universe is nineteen, `prices/live` serves nineteen, and the snapshot the phone reads serves seventeen. The route the app depends on is the odd one out | — **one integer.** `BLOCKED.md §א20` |
| 49 | One thing to change on the server | ❌ **named, not fixed — it is the server's to do** | `/api/health` prints each upstream's private address (`10.10.1.2`, `10.10.1.3`) to the public internet. Neither is a secret nor reachable from outside, but it is a network diagram for anybody who asks and it buys a reader nothing: `reachable`, `probe`, `latency_ms` and the cache rate are the fields that matter. Drop `address`, or keep the detailed route private and serve a bare `{"status":"ok"}` publicly |

---

## א10 — the socket, and the two walls behind it

Phase 3 is built. It is **forex-complete and crypto-closed**, and both halves of that are facts
about the backends rather than about the relay.

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 50 | One upstream connection, two clients | ✅ | Two browsers on EURUSD for 150 s → **one** TCP connection from the relay to CoinePro-FX, counted from `/proc/net/tcp` inside the relay's own container rather than from its own statistics. Both clients received the same tick, identical timestamp and last. The ceiling **refuses** rather than truncates: 201 symbols is an error frame, 200 is accepted, a second socket from one address is `403` | — **two integers, and where they were counted from.** `SERVER.md` §4.11 |
| 51 | §4.2 assumed a crypto socket that does not exist | ✅ **spec corrected** | Seven candidate paths on TradeYar: two time out at the handshake, three answer `404`, and the two that resolve close `4001` / `4401 unauthorized` — the second being the one the Android app opens. So the crypto socket is the §4.0 wall again: authenticated surface, and a relay with no account can open **zero** connections, not one | — **seven probes.** §4.2 |
| 52 | The venue that is not live says so, in the first frame | ✅ | `{"type":"welcome","venues":{"forex":{"live":true},"crypto":{"live":false,"reason":"upstream requires an account…"}}}`. **The difference between a chart that says «there is no crypto feed» and a chart that simply never ticks is this one frame** — and a silently dead feed is the failure this product has spent three runs designing out | — **a protocol frame, now in §4.2** |
| 53 | Crypto on the socket: bridge it, or wait? | ✅ **decided: wait** | The relay could poll the public route every 2 s and push the differences. It would relay values unchanged — but it would invent the one thing a tick is *for*, **when it happened**, carrying either the poll's own clock or an upstream timestamp up to 2 s late with nothing saying so. Rule 1 again. And refusing costs nothing: a tab polling `/api/crypto/prices` is the **same upstream traffic**, because the 2 s cache collapses it either way. §4.2.1 records the argument and the two notes the terminal needs | — **a decision, and an argument a still cannot carry** |
| 54 | A third forex universe, and gold is in none of the small ones | ✅ **measured** | `prices/live` 19, `ws/snapshot` 17, **the socket 7** — the majors and nothing else. `XAUUSD` gets a snapshot value and **never another frame**. §א20's ask is two lines now rather than one | — **three counts.** §4.10, `BLOCKED.md §א20` |
| 55 | The forex quotes are Yahoo Finance | ❌ **named, and the app must not paper over it** | `source: "yfinance"` on all seventeen rows, with `bid == ask == price` exactly — no spread, one number in three fields. The app already files them under `QuoteSource.UNKNOWN`, so nothing on screen is mislabelled; but **the chart beside them names «MetaTrader 5»**, which `CandleGateway.sourceName`'s KDoc prints precisely so a reader can check the claim. Two venues on one screen, one named. Adding `YFINANCE` to the enum would be this run deciding Yahoo Finance is an acceptable quote source for a paid product — the owner's call. `BLOCKED.md §א22` is the question |
| 56 | `SERVER.md` said «over Finnhub» for a year | ✅ **corrected** | §1 of both the spec and the build brief. It was never measured; it is now, and §4.10 carries the reading with its date | — **one sentence in two files** |
| 57 | The weekend measurement, kept because it explains a frozen chart | ✅ | The forex market is shut at the weekend: upstream sends a frame a second with unchanged values — 45 frames, zero change — and re-samples about every two minutes. The relay forwards **changes**, not frames, so a closed market costs nothing downstream and a tick is real even when its price has not moved | — **a rate and a duration.** §4.11 |
| 58 | `/api/health` no longer draws the network | ✅ | The public fields are `reachable`, `probe`, `latency_ms`, `last_good_age_s`. No address, no host | — **an absence.** §4.9 asked for it; the server did it |
| 59 | The rate limit that §4.2.1 broke, and that I wrote | ✅ **corrected** | §6 said 60 a minute per IP on `/api/*`. Then §4.2.1 made the terminal poll crypto every 2 s — **30 a minute, so two tabs from one address exhaust that route alone**, before candles or news take a share. And per-IP is blunt in this market specifically: `NetworkFactory`'s KDoc already says the backends cannot rate-limit the phone by address because carrier-grade NAT puts a very large number of Iranian subscribers behind one. The relay's author did the arithmetic and did **not** change the number unilaterally, which was right — it was mine to fix | — **a table of buckets.** `SERVER.md` §6 |
| 60 | Limits proportional to what a request costs | ✅ | Cached reads (both price routes, the update document, the legal pages) get **240 a minute**, because they are served from a 2 s Redis cache and cost the upstreams *nothing* — a hundred tabs are one upstream call either way, so refusing them protects a resource that is not scarce. Everything else stays at 60, because a candle miss really does reach a backend. Bucketed by `(address, client id)` where the browser sends one, the way the app sends `X-Install-Id` — widening for honest readers behind one NAT, never replacing the address ceiling, since a browser can mint identifiers | — **four numbers and the reason for each** |
| 61 | What the update digest actually proves | ✅ **written down, because it is not the obvious answer** | One machine now serves both the document and the APK it names, so the `sha256` proves **transport, not publication**: anyone with root there could serve a different file and a matching digest. The anchor is the **signature**, and it is on the phone — Android refuses an install over an existing app unless the certificate matches, and the signing key lives in CI, not on that host. So a compromised `pro-chart.com` can at worst offer a package that fails to install. Three consequences follow, including why the GitHub release stays and why the key must never move to the server | — **a threat model.** `DISTRIBUTION.md` §4½ |

---

## א11 — the web target, because «when is the web ready» has to have an answer with a number in it

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 62 | The engine compiles for a browser | ✅ | `:chart-core:compileKotlinWasmJs` is green and runs in `android-ci.yml` on every push. Scales, 83 indicators, 85 drawing tools' geometry, the backtester, the object tree, the replay — all of it now **builds for WebAssembly**, which is a fact rather than the argument `PLAN.md` §2 used to make. The five `actual`s are in `ChartPlatform.wasmJs.kt` and use no `Intl`: an axis label and a Fibonacci ratio are Latin digits with a `.` and English month names, so `toFixed` and a pattern printer over the `CivilDate` already in common code give the same bytes `Locale.US` gives on the phone | — **a green compile task, in CI** |
| 63 | The language compiles for a browser | ✅ | `:namascript:compileKotlinWasmJs`, same job. The lexer, the parser, the type checker, the interpreter and the 61 shipped strategies build for WebAssembly unchanged | — **a second green task** |
| 64 | The one surprise, and it was a real defect | ✅ **fixed on every target** | `NamaScript` caught `StackOverflowError` so an over-nested script became `E405` rather than a crash. On the JVM that is sound. In WebAssembly an exhausted stack is a **trap**, which cannot be caught — it would have taken the whole terminal page down, not the script. So the parser now has its own limit (`Parser.MAX_NESTING` = 128) that refuses before the recursion starts, **with a line and a column**, on every target; the JVM's catch stayed behind an `expect`/`actual` that is honest about the difference rather than a `catch (Throwable)` that would report every genuine bug as «nested too deeply». Two tests: past the limit is `E405` with a caret, just under it compiles | — **a crash path closed on the phone too.** `DeepNesting.kt` |
| 65 | The web cannot leak into common code either | ✅ | `kotlin.js.` and `kotlin.wasm.` join `java.`, `javax.`, `android.` and `androidx.` on both `ArchitectureTest`s' forbidden list. The rule was never «no Android»; it is «nothing that is not on every target», and until today one half of that had nothing to enforce it | — **two lists, symmetrical** |
| 66 | «When is the web ready, at parity with TradingView?» | ✅ **answered with a schedule and a refusal** | `docs/web/PARITY.md`. Parity is three promises, not one: the **chart** (18 chart types, 83 indicators, 92 tool-rail entries, a language with 351 conformance scripts — reachable, and mostly already reached); the **terminal** (a port of surfaces that exist, which is what the 18–28 days buys); and **the network** — their data licences, their community, their brokers — which is **not reachable and mostly not wanted**, and saying so is the part that makes the rest trustworthy. W1+W2+W3 is a usable terminal and nothing blocks it. W4 onwards is behind one question only the owner can answer | — **a matrix, a phase table, and one number: 18–28 working days** |
| 67 | The parity rig gets easier on the web, not harder | ✅ | `TRADINGVIEW_FINAL_PARITY_REPORT.md`'s verdict is `NOT YET PROVEN`, blocker `REFERENCE_MISSING`, because capturing their **Android** app needs a device with it installed and no build host has one. Their **web** terminal runs in a browser a build machine also runs — same window, same DPR, same screenshot pipeline. The comparison that is impossible today is ordinary once there is a page to compare | — **why W6 is short** |
| 68 | Where the terminal mounts, and why not at the root | ✅ **decided and specified** | `/terminal/`, with the SPA fallback scoped to that one prefix. Rooted at `/` the fallback is a **denylist** — every existing path has to be excluded from it, and so does every path a future author adds without knowing the rule. Rooted at `/terminal/` it is an allowlist, and a typo outside it is an honest `404` rather than a chart where a legal document should be. The root keeps a more urgent job: with no app store serving these readers, a first install is a hand-carried APK, and `DISTRIBUTION.md` §4½ says that reader's only protection is a fingerprint published where they will look — which is the address they were given | — **a path table.** `SERVER.md` §3.1 |
| 69 | `COOP`/`COEP`: no, and not as an omission | ✅ | Those two headers exist for `SharedArrayBuffer`, which exists for shared-memory threads, and **Kotlin/Wasm has no threading model** — no `Thread`, no shared-memory atomics, coroutines on `wasmJs` dispatch on one thread exactly as on `js`. Adding them «to be safe» is not safe: `COEP: require-corp` breaks every cross-origin resource that has not opted in, so guessing wrong that way is a blank page, while guessing wrong the other way is a bundle that refuses to start and says so in the console. **The bundle is the test** | — **a decision with its own falsifier** |
| 70 | The share address, before anyone builds it | ✅ **reserved, with two constraints written down** | `/s/<id>` is **not** what the app's board sharing uses — the board refuses URLs at the door, so a script travels as the `.nama` document inside the post (`ScriptShare`). The address is for a link pasted into Telegram, where the link's whole job is the Open Graph card in the HTML head — which an SPA route cannot have. So: server-rendered, never an SPA route. And `ScriptLink` already carries an **id, never source**, so nothing runs on a tap | — **two constraints.** `SERVER.md` §3.1 |
| 71 | Two things that will be mistaken for faults | ✅ **pre-empted in the spec** | `HEAD /api/app/latest` reports `content-length: 20` from the edge and 989 from the origin — Cloudflare answers a `HEAD` with the compressed length, its ETag carrying `-gzip`; the app uses `GET` and receives the whole document. And the legal pages moved from `max-age=300` to `no-cache`, which is right for a document whose entire value is being current, at the cost of one revalidation and a `304` | — **§6.3, written so a future `curl -I` does not start a fire drill** |
| 72 | The legal pages, re-measured — and the third one is not what §3.1 first said | ✅ **corrected in the spec** | The host serves `/legal/terms/`, `/legal/privacy/` and `/legal/delete-account/`, all `200`, all with the **trailing slash canonical** and the bare form answering `308` to it. There is no `/legal/risk/` and there never was — an invention of mine, caught by probing instead of by re-reading. The app already builds every one of these with the trailing slash (`MembershipGate`, `MembershipJourneyPanel`, `DeleteAccountScreen`), so the `308` costs a reader nothing | — **six status codes.** `SERVER.md` §3.1 |
| 73 | `/reset` verifies as an App Link and answers `404` | ❌ **named — it is the server's page to build, and it is not urgent yet** | `assetlinks.json` is right, so the claim verifies and a phone with the app installed opens the app. Everybody else gets a `404`. **No e-mail names that address today** — CoinePro-FX's reset mails name `coineprofx.com/reset-password`, claimed separately — so nothing is broken for a reader; what is broken is the symmetry, and the page must exist before any mail names it. `APP_LINKS.md`'s paragraph said the host «does not answer on 80 or 443», which stopped being true and was hiding this | — **one status code, and a stale paragraph rewritten** |
| 74 | `/reset`: three options came back, and the premise of the cautious one was a misreading | ✅ **decided: build it, one route proxied** | The relay's author read §4.4 as «do not proxy auth until the owner answers». §4.4 says the opposite, and always did: **the default is written down** — auth proxies to CoinePro-FX until §8.1 is answered, which is where `RESET_HOST` already points. So `POST /api/auth/password/reset` is not Phase 4 taken early; it is the row above, followed. A mail carrying a `pro-chart.com/reset` link would be CoinePro-FX's mail carrying CoinePro-FX's token | — **one table row that had been read backwards.** `SERVER.md` §4.4 |
| 75 | Why not the static half, and why not the `404` | ✅ | A page that renders a form it cannot submit is **worse than a `404`**: the `404` is unambiguous, while a dead form teaches a reader to distrust the product at the one moment they are already locked out. And the `404` is only safe while nobody changes a mail template — a decision taken in another repository by people with no reason to know the page is missing. **A thing that is correct only while somebody else does not act is not correct** | — **two rejected options, each with its own reason** |
| 76 | «§6.2» now names two different things | ✅ **fixed by naming** | This document grew a §6.2 about serving the bundle while `PLAN.md` §6.2 is «which backend owns the account» — one a cache header, one an owner decision, and the relay's author cited the number alone. `SERVER.md` §8 is now declared the canonical list and says so at its head | — **a collision I made, closed the next day** |
| 77 | The reset credential is a typed code, and the page was built for a link | ✅ **found by reading CoinePro-FX's source, not by asking** | Two reset flows, different credentials. **Academy**: `secrets.token_urlsafe(32)` — 43 characters — in a link at `…/reset-password?token=`, consumed by `POST /auth/reset-password`. **Mobile** — the flow this app and the relay use: an **eight-character code** `ABCD-EFGH` in the mail body, **no link at all** (`src/api/mobile/reset.py`, brand `panel_reset`), consumed by `POST user/auth/password/reset {reset_token}` at 6–40 characters. So a page that shows its form only when `?token=` is present has, for every reader today's mail produces, no form — and «open the full link from the e-mail» sends them looking for something the e-mail does not contain | — **two routes and two token shapes, read out of the backend** |
| 78 | The app promised a link that the e-mail does not contain | ✅ **fixed, both languages** | `auth_reset_body` said «اگر لینک ایمیل را روی همین گوشی باز کنید، کد خودش پر می‌شود» / «Opening the emailed link on this phone fills it in for you». The `panel_reset` template sends the code and nothing else. The sentence is gone; the deep-link path stays wired, because it is correct the day a mail carries one — what was wrong was describing it as though that day had come | — **two strings, and the template that disproves them** |
| 79 | The reset says one more thing than «the password changed» | ✅ **added, both languages, both surfaces** | The backend calls `revoke_all_for_user(user.id, "password_change")` — **every other session is signed out** — and neither the app nor the new page said so. A reader who resets on a laptop and then finds their phone logged out should have been told, and it is the backend's own behaviour rather than an invention. `auth_notice_password_changed` now carries it, and §3.1 gives the relay the same two sentences verbatim so the page and the app speak once | — **one function call nobody had read aloud** |
| 80 | `{"reset": true}` rendered as a sentence — is that authorship? | ✅ **no, and the line is now written down** | Rule 1 forbids the server **authoring a claim**; turning a flag the backend *did* set into a readable sentence is translation, which is what every screen in the app does. Two conditions keep it honest: the test is `HTTP 200 && body.reset === true` and never the truthiness of `body.reset` — a `{"reset": false}` or a 200 carrying an error shape must not read as success, because a reader told «done» when nothing happened will not try again — and the sentence says only what the backend did | — **a rule with its own edge case.** `SERVER.md` §3.1 |
| 81 | W1 estimated, then counted | ✅ **audited import by import** | `:chart-ui` is **15,242 lines across 17 files and 22 platform-bound imports**. Seven of those (`LocalDensity`, `LocalLayoutDirection`) cost **nothing** — Compose Multiplatform has them under the same names. Five are `painterResource`/`stringResource`. Seven are **two files**: `ChartFrameRate.kt` (69 lines, `SurfaceControl.setFrameRate`) and `ChartStrokePredictor.kt` (59 lines, `MotionEventPredictor`), both `expect`/`actual` with a web half that does nothing. `CoineProChart` at 7,429 lines, the drawing renderer at 2,576 and all eighteen series types draw on `androidx.compose.ui.graphics.Canvas`, which is the same API in a browser. **128 lines are the only Android in the chart** | — **a 22-row import census.** `PARITY.md` §3a |
| 82 | The item nobody had counted, found before it cost a fortnight | ✅ **W1½ added, and the estimate went up** | `core/designsystem/src/main/res/drawable/` holds **1,150 vector XML files** — every asset logo, every tool glyph. Compose Multiplatform reads Android `<vector>` XML, so most move by copying; but **226 of them use `aapt:attr` with an inline `<gradient>`**, which is a resource-linker feature rather than a vector one and does not render anywhere else. 218 use `fillType` and 11 use `<group>`, both fine. The web estimate went from 18–28 days to **21–33**, which is the audit doing its job: W1½ was inside nobody's estimate, while W1 itself turned out smaller than it looked | — **four counts out of 1,150 files** |

---

## א12 — both blockers closed themselves, and one measurement of mine was wrong before it was right

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 83 | §א20 — gold | ✅ **closed by the backend, unprompted** | The bare `GET api/ws/snapshot` returns **19** with `XAUUSD` and `XAGUSD` among them — the same 19 `api/public/prices/live` serves, so the two routes agree for the first time — and the forex socket carries **9** rather than 7. The headline instrument has a live stream. **No message was ever sent to CoinePro-FX's team**; it was written down, measured, and left alone on the principle that a symbol the feed does not list is a symbol the app must not invent | — **two counts and a symbol list, read from `coineprofx.com`** |
| 84 | §א22 — the venue | ✅ **half closed; the app needed no change** | `source` is **`finnhub`** on all 19. `MarketDataController` has always mapped that string, so these quotes move from `QuoteSource.UNKNOWN` to `FINNHUB` on their own and inherit the 90-second staleness window instead of 30. **This is what refusing to add a `YFINANCE` enum bought** — the app had not encoded somebody else's mistake, so there was nothing to migrate | — **one enum that did not have to change** |
| 85 | What §א22 leaves behind | ❌ **named, not blocking** | `bid == ask == price` on **19 of 19** — one number echoed into three fields, so this feed still carries no spread. And `CandleGateway.sourceName` still prints «MetaTrader 5» beside a quote that now says Finnhub. Two venues on one screen is legitimate — candles and quotes need not share a source — but only one of them is named, and the label exists so a reader can check | — **an equality that held across both measurements** |
| 86 | My first reading said the snapshot was **empty**, and it was my parser | ✅ **caught by re-measuring before reporting** | `prices` is a JSON **object keyed by symbol**, not a list; a walker that looked for the first list found none and printed `count=0`. Had that gone out it would have read as a catastrophic regression — the app's whole forex catalogue gone — and sent somebody hunting a fault that did not exist. **The habit that caught it is the one this run has used throughout: measure, then measure differently before believing the alarming answer** | — **a bug in the measuring instrument, not the thing measured** |
| 87 | §8.1 answered — TradeYar owns the account | ✅ **and the switch cost what §4.4 predicted** | Open since `SERVER_ASK_ONE_ACCOUNT_TWO_BACKENDS.md` was written. The reset route moved the same day: the body is `{reset_token, new_password}` on both backends, so only the destination changed. **W4 and server phase 4 are unblocked** | — **one row of §4.4, rewritten from a default into a decision** |
| 88 | §8.2 answered — open, read-only, no account | ✅ **the answer that changes the schedule most** | It needs no work: it is exactly what Phases 2 and 3 already serve. And it means **`PARITY.md`'s W1 → W3 ships to readers on its own** — there is nothing to sign in to, so the chart, the tools, the scripts and the watchlist no longer wait behind W4 | — **a dependency removed from a table** |
| 89 | The sentence removed, then read, then restored | ✅ | «Every other device was signed out» came out of CoinePro-FX's source. When the destination became TradeYar the relay's author dropped it, having read nothing of TradeYar's — **right, because a claim kept by habit about a server nobody has looked at is inventing one with extra steps**. Then somebody looked: `password_reset` in TradeYar's `app/api/routers/mobile/auth.py` calls `revoke_all_for_user(platform_id, reason="password_changed")` **unconditionally** before it returns. The sentence goes back because it was read, not because it was already written | — **one function call in another repository** |
| 90 | What must not be said, though it is true | ✅ **named so nobody adds it later** | TradeYar also sends a «your password changed» mail — the message that exposes a takeover. It is conditional on the account having an address and wrapped in a `try/except` that logs and swallows a failure, deliberately, so a mail server cannot fail a reset that already succeeded. **A page promising that mail would be lying in exactly the case where it matters** | — **a `try/except` read as a contract** |
| 91 | The untested branch, answered from source | ✅ | The relay could not see TradeYar's success shape without a real token and said so rather than guessing. It is `{"reset": True}`, returned at the end of the same function — so the inference from `MobileAuthApi` was right, and now it is read rather than inferred | — **one `return` statement** |
| 92 | And the argument that turned out to be TradeYar's own | ✅ | Two days ago this run refused to leave `/reset` a `404` because «a thing that is correct only while somebody else does not act is not correct». TradeYar's mailer makes the same argument from the other side, in a comment, about the same link: it is never given a guessed default **because a link that 404s is worse than no link**. The page existing first is what lets that variable be set at all | — **two repositories reaching the same rule independently** |

---

## א13 — W1 started, because asking was the thing stopping it

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 93 | «چرا سایت لانچ نمی‌شود؟» — and the site is launched | ✅ **measured before answering** | `pro-chart.com` serves a real page: «نمودار، سیگنال و تحلیل بازار — طلا، فارکس و رمزارز — روی اندروید», the **دریافت اپ — نسخهٔ 5.0.0+4** button pointing at `/download/pro-chart-5.0.0-b4.apk`, and the three legal links. What is not launched is `/terminal/`, and the two are different things that the same question can mean | — **4,170 bytes of HTML, read rather than assumed** |
| 94 | Why it had not started, and it was not a technical reason | ✅ **named** | Three replies in a row ended «شروعش با توست؛ خودسرانه شروع نمی‌کنم» — which is the «shall I continue» that `CLAUDE.md` forbids in as many words, dressed as deference. The estimate was done, the audit was done, the decisions were answered, and the thing that was missing was somebody starting | — **the owner had to ask** |
| 95 | W1a — `:chart-ui` is a KMP module | ✅ **green** | `com.android.kotlin.multiplatform.library` with one target, Android; 17 files moved to `androidMain`, the Robolectric suite to `androidHostTest`, a `testDebugUnitTest` alias so the repository's gate keeps its name. **The whole test suite and `:app:assembleRelease` pass, and no behaviour changed** — the module's 15,242 lines were not touched | — **two green Gradle invocations** |
| 96 | Why the module moved before the target did | ✅ | Adding `wasmJs` to an Android library is a rewrite; adding it to a KMP module is a source set. Splitting those two in time means the browser target lands on a build that is already green in its new shape, and a `commonMain` can be lifted out from under `androidMain` **a file at a time with the build green after each one** — which is how `:chart-core` reached the browser without a single red commit | — **the pattern that already worked once** |
| 97 | The server's brief, audited from outside after its agent's session died | ✅ **complete — nothing was left half-done** | `/`, the three legal pages, `assetlinks.json`, `/api/app/latest`, `/reset` all `200`. The relay: 858 crypto rows, 19 forex with gold and silver, news 20, showcase, community, membership, track-record, crypto candles. FX candles **M15 H1 H4 D1 → 200 and M5 M30 W1 → 422 passed through byte-for-byte**, which is §4.8's rule that a refusal is not translated. The socket's welcome frame is exactly the specified shape. Auth: `password/reset` reaches TradeYar and **every other `/api/auth/*` is 404**, which is the one-route scope holding | — **twenty-odd probes against the live host** |
| 98 | My own probe was wrong again, and again it was caught before it was reported | ✅ | `/api/fx/candles?…&limit=5` answered `422` and read like a regression against §4.8's «H1 → 200». The `422` was the upstream's own: `limit` must be ≥ 20. **The relay was right, the spec was right, and the test was wrong** — the second time in two days that an alarming reading came from the measuring instrument | — **one query parameter** |
| 99 | W1½ was five days of work that does not need doing | ✅ **measured, and the estimate collapsed** | The first line of every drawable says `<!-- Generated by scripts/design/svg-to-vector.py from design/… -->`. The XML is an **output**, the SVG sources are in this repository — **5,021 of them** — and **1,117 of the 1,150 drawables still have theirs, including all 226 with `aapt:attr` gradients**. Compose Multiplatform reads SVG on every target, so the browser reads the sources and Android keeps the XML it ships today. Nothing to convert, no gradient to flatten | — **three counts: 5,021, 1,117, 226 of 226** |
| 100 | What W1½ actually is now | ✅ **named and bounded** | **33 hand-authored XML icons with no SVG behind them** — `tv_tool_eraser`, `tv_zoom_in`, `tv_chart_footprint` and a handful of chart-type glyphs. One day, not five. Twice now a measured number has replaced an estimated one and the answer got smaller; the pattern is that **the estimates were expensive because nobody had opened the files** | — **a list of 33 filenames** |
| 101 | One stale string on the live server | ❌ **named for the next deploy, not a fault** | The socket's welcome frame explains the dead crypto venue with «upstream requires an account (PLAN.md §6.2, Phase 4)». The **fact is still true** — TradeYar's crypto socket needs an account — but the citation points at a number this run retired as ambiguous and at a decision that has since been **answered**. `SERVER.md` §8.1 is the canonical name | — **one reason string in a JSON frame** |
| 102 | §8.1 answered a second time, the other way | ✅ **spec rewritten, and the reversal cost one route** | «TradeYar» on 2026-09-21; **«neither — Pro Chart's own»** on 2026-09-22. The year-old question in `SERVER_ASK_ONE_ACCOUNT_TWO_BACKENDS.md` asked *which of the two*, and the owner answered by **refusing its premise**. Every answer to the original question made the product's front door somebody else's property: a reader who wants a chart would have an account on a crypto venue's server. The proxy added yesterday is removed — which is what «one route, reversible in an afternoon» was written for | — **`SERVER.md` §4.4 and §8.1, rewritten** |
| 103 | What the reversal makes this machine | ✅ **named before it is built** | Until now `pro-chart.com` held nothing that could hurt a reader if it were lost — a cache, three static documents, a public APK. From the first account it holds **password hashes, e-mail addresses and session tokens**, and becomes worth attacking. `ACCOUNT.md` §3 is seven rules that exist because of that one sentence: Argon2id with its parameters in the migration, per-account backoff on top of the per-address limit, the same answer whether the address exists or not, reset tokens hashed and single-use and never consumed by a `GET`, and a delete that deletes rows rather than setting a flag | — **a threat model written before the table** |
| 104 | The question the decision does not answer | ❌ **named, and the safer half specified** | A reader's **entitlement** — whether they are a paying member — lives on the backends and should stay there; billing belongs where the service is sold. So a Pro Chart account needs to say «this reader is also that reader», and there are two shapes: **link on demand** (reversible, stores `upstream_user_id` and nothing secret) or **migrate** (cleaner, irreversible, needs both backends' teams). The brief says build the first and not the second. And the screen must say that an unlinked account is a chart account, **not** a subscription, rather than leaving a reader to discover it | — **two shapes, one chosen.** `ACCOUNT.md` §4 |
| 105 | The dependency this creates that no amount of work removes | ❌ **blocked on the owner** | This server has never sent an e-mail, and a reset flow is nothing without one. It needs a sending domain with **SPF, DKIM and DMARC** and a provider. **A reset mail in a spam folder is a reset that did not happen**, so the brief tells the agent to stop and ask rather than guess a relay | — **one sentence that stops a phase** |

---

## א14 — Cafe Bazaar refused 4.88.0, and the screen was worse than the fault

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 106 | The refusal, quoted rather than paraphrased | ✅ | «با انتخاب گزینهٔ «ورود یا ساخت حساب»، پیغام «پاسخی نرسید. این یعنی درخواست بررسی نشد» نمایش داده می‌شود.» — `version code: 48800000`, `arm64-v8a, armeabi-v7a`. A reviewer opened the sign-in screen, one request did not answer, and the store stopped there | — **one screenshot** |
| 107 | The dead screen, which is the part that was ours | ✅ **fixed** | `loadMethods()` asked the backend which ways in exist. On failure it set nothing, and the screen drew **no buttons at all** — not a failed attempt, no attempt. The reasoning was sound about Google and Telegram (both need something discovery supplies) and wrong about e-mail, which needs no configuration to try. It now falls back to `AuthMethods(emailPassword = true)` with `methodsAssumed = true`, and the screen says so: «نشد از سرور بپرسیم کدام روش‌های ورود فعال‌اند … اگر نشد، ایراد از سمت ماست» | — **`EmailAuthController.loadMethods`, two tests** |
| 108 | «پاسخی نرسید» was false of half the cases it was shown for | ✅ **fixed** | `ErrorKind.SERVER` — a 5xx — mapped to `UNREACHABLE`, whose string says «درخواست بررسی نشد». That is **false** of a 500: the request arrived, it was read, and the fault is ours. It sends a reader to check their connection while the server is the thing that broke, and told the reviewer nothing anybody could act on. `SERVER_FAULT` is now its own reason with its own sentence | — **`AuthFailureMapping.kt`, `AuthFailureMappingTest`** |
| 109 | And the third case, which is the opposite of «no answer» | ✅ **fixed** | A certificate this build refuses is an `SSLPeerUnverifiedException`, which is an `IOException`, which arrived as `NETWORK`, which read as «پاسخی نرسید» — the same sentence a phone in a tunnel gets. The server answered the handshake and **we** hung up. `UNTRUSTED` says that, and says the true consequence: trying again will not help, the app has to be updated. The cause chain is walked, because OkHttp wraps a retried route's failure | — **three tests, one of them a looping cause chain** |
| 110 | What none of this proves | ❌ **blocked on the owner — one measurement, off this machine** | The three fixes make the next failure legible; they do **not** establish what today's is. It cannot be established from here: this sandbox's egress proxy re-terminates TLS, so every chain measured reads `issuer=O = Anthropic, CN = Egress Gateway …` and every SPKI digest is the proxy's key. One datum survives re-termination, because the proxy copies the upstream subject: `tradeyar.trade-future.ir` now presents **`CN = *.trade-future.ir`**, a wildcard — where `DEFAULT_CERTIFICATE_PINS` pins a per-host leaf SPKI from a certbot with `reuse_key = True`. The leaf pin surviving a move to a wildcard is not something to assume | — **a subject line, and an instrument that cannot read the rest.** ~~The wildcard~~ — see row 122: the subject was the proxy's too, and the claim was wrong |
| 111 | The commands that answer it, from anywhere but here | ❌ **for the owner** | `openssl s_client -connect tradeyar.trade-future.ir:443 -servername tradeyar.trade-future.ir < /dev/null 2>/dev/null \| openssl x509 -noout -issuer -subject -dates`, then the same piped through `openssl x509 -pubkey -noout \| openssl pkey -pubin -outform der \| openssl dgst -sha256 -binary \| openssl enc -base64`. If the digest is not `RO8XwxTQ…` **and** the issuer is not Let's Encrypt, both pin sets are stale and every install of 4.88.0 has been refusing its own server since the certificate changed — `app/build.gradle.kts` `DEFAULT_CERTIFICATE_PINS` and `app/src/main/res/xml/network_security_config.xml`, held equal by `NetworkSecurityPinsTest` | — **two commands, four lines of output** |

---

## א15 — the caption that named one venue and called it the other's

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 112 | Row 85 closed, and it was a false sentence rather than a missing one | ✅ | The band under the plot read **«منبع قیمت: MetaTrader 5»** — *price source* — and the string was filled from `CandleGateway.sourceName`, which names the **candles**. On forex the last price above those bars arrives from the snapshot feed reporting `source: "finnhub"`. So the one caption whose whole job is provenance was naming the wrong feed for the number a reader looks at first | — **one format string, `chart_source`** |
| 113 | Why that is the specific fault the label exists to prevent | ✅ | `CandleGateway.sourceName`'s own KDoc: the accusation is «کندل‌سازی», it is unanswerable without provenance, so «every gateway names its venue, the chart prints it, and the claim becomes falsifiable — a reader can hold this chart against that venue's own». A reader who did exactly that, against **MetaTrader 5's prices**, was checking a feed this screen was not showing them, and any difference they found proved nothing either way | — **the doctrine, applied to itself** |
| 114 | Both venues, read off the feed rather than assumed | ✅ | `QuoteSource.displayName`; `PriceTick.sourceName` carried from `MarketDataController`'s own mapping of the feed's `source` field; `ChartController.noteQuoteVenue` recording it; `ChartUiState.quoteSourceName` drawn beside the bars' name. It is **not** derived from the platform, because the platform does not know — a backend that changes quote provider changes that one field and nothing else | — **four tests in `ChartQuoteVenueTest`** |
| 115 | The three cases where it says nothing | ✅ **the part that keeps the line worth reading** | Same venue as the bars — crypto, LBank under LBank — prints one line, not the same name twice. An unrecognised `source` prints **nothing**: `QuoteSource.UNKNOWN.displayName` is empty rather than «نامشخص», because a wrong venue name is worse than none — a reader can act on it. And a tick for another market cannot rename this one's | — **`QuoteSourceNameTest` forbids a placeholder name** |
| 116 | What is still open, unchanged | ❌ **named, not blocking** | `bid == ask == price` on **19 of 19** rows: one number echoed into three fields, so this feed still carries no spread. That is the backend's, and §א22's remaining half | — **an equality that has now held across three measurements** |

---

## א16 — the version stopped saying «+۱» (5.0.4)

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 117 | The owner's decision, and the reason that replaced a good one | ✅ | The device, the APK's filename and the release title all read `5.0.3+1`. The reasoning for `+N` was sound — a bug report should name the exact binary rather than the nearest version — and it was overruled by a better one: **a version is what a person says out loud**, reads on a store listing and types into a support message, and nobody says «five point oh point three plus one». The build after 5.0.3 is 5.0.4 | — **`full = name`, and `tag = v{name}`** |
| 118 | What `+N` was actually protecting, met the other way round | ✅ | Two binaries must never share a version name. `+N` made the name unique; `--release` makes the situation impossible — it is the publishing workflow's first step and it fails when there are commits on top of the version in `version.properties`, **before** twenty minutes of building rather than after. Its message names the three things to do | — **`release_guard`, wired at `android-apk.yml`'s Compute version step** |
| 119 | `BUILD` did not go anywhere, and that is the point | ✅ | It still rides in `versionCode`, which is the only place precedence is actually decided and the one place no reader looks. `50004000` clears `50003001`, so every device holding the last build takes this one | — **two integers** |
| 120 | The off-by-one that would have broken every pre-commit gate | ✅ **found by running it** | `build_number()` counts commits since `version.properties` last changed **in git**, so straight after `--bump patch` — file modified, not yet committed — it still answered the old count. `--name` then read `5.0.4` while the gates computed against a version one higher, and `check-update-notes.py` would have demanded a card for a version that will never exist. A modified-but-uncommitted version file now answers `0`, because the pending commit **is** the change the number counts | — **`git status --porcelain -- version.properties`** |
| 121 | And the pre-release flag, which was reading the wrong thing | ✅ | The GitHub Release was marked pre-release on `build != 0`. With the guard in place that is always false, so the flag would have said nothing. It now asks semver's own question — does the name carry a `-rc.1` — which is what «pre-release» has always meant and what `PRE_RELEASE=` in `version.properties` sets | — **one workflow expression** |

---

## א17 — the wildcard was the instrument's, and it was the third time

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 122 | Row 110's one surviving datum did not survive | ❌ **my claim, withdrawn** | Row 110 said the proxy re-terminates TLS but copies the upstream **subject**, and reported `tradeyar.trade-future.ir` now presenting **`CN = *.trade-future.ir`** — a move to a wildcard, which would explain a leaf pin no longer matching. The owner measured it from outside: `subject=CN = tradeyar.trade-future.ir`. **Per-host, exactly as the pin assumes.** The proxy does not copy the subject faithfully either, so the one field I treated as trustworthy was not | — **the owner's own `openssl` output** |
| 123 | Three times now, and the pattern is the same one | ✅ **named, because it keeps happening** | `ws/snapshot` read as empty (it is a dict, not a list). `fx/candles?limit=5` read as a regression (the upstream floor is 20). And now a wildcard that was never there. Every one came from **the measuring instrument**, every one looked like a server fault, and the two caught before reporting were caught by re-reading the source rather than by doubting the number. This one was reported — the difference is that nothing in this sandbox could have caught it | — **a rule: through the proxy, only the HTTP body is evidence** |
| 124 | What the owner's measurement does settle | ✅ | The issuer is `O = Let's Encrypt, CN = YE2` and the subject is the host itself. So the certificate is Let's Encrypt's, for this host, and the handshake completes — the server is healthy and the host is reachable. What it does **not** settle is whether the app's pins match it, because a pin is a digest of a **key**, and neither an issuer name nor a subject is one | — **two lines of a certificate** |
| 125 | The one measurement still outstanding, and why it is the decisive one | ❌ **blocked on the owner — one command** | `DEFAULT_CERTIFICATE_PINS` holds four digests for this host: the leaf's SPKI, an offline backup key, and ISRG Root X1 and X2 as backstops. The leaf pin was measured at 4.57.0 against a certbot configured `reuse_key = True`, so it survives renewal **only while that setting holds**; the roots catch everything that still chains to ISRG. `YE2` is an intermediate this repository has never seen, so the question is whether the chain still ends at one of those two roots. The SPKI digest and the chain answer it; nothing short of them does | — **`openssl x509 -pubkey \| openssl pkey -pubin -outform der \| openssl dgst -sha256`** |

---

## א18 — pinning is cleared, by measurement, and the refusal has one hypothesis left

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 126 | The leaf pin has survived every renewal since 4.57.0 | ✅ **exact** | The owner's measurement of the live SPKI is `RO8XwxTQmKWLxQ7Ij7dkTd5vWTS4aC2pROWNg3Sh25c=` — **byte for byte** the digest in `DEFAULT_CERTIFICATE_PINS` and in `network_security_config.xml`. The server's certbot still has `reuse_key = True`, which is what that pin was written on the strength of and what it has now been shown to be right about | — **two base64 strings, compared character by character** |
| 127 | And the chain still ends where the backstops are | ✅ | `leaf ← YE2 ← ISRG Root YE ← ISRG Root X2`, with `ISRG Root X1` sent for older clients. **Both roots are pinned.** So two independent things would have had to fail before an install refused this server — the leaf key *and* the root — and neither did | — **four lines of `-showcerts`** |
| 128 | What that makes the Cafe Bazaar refusal | ❌ **one hypothesis left, and it is not the app's** | The server answers, the certificate is valid, the pins match. «پاسخی نرسید» was an `IOException` that was neither a pin nor a dead server, so what is left is the route between an Iranian reviewer's device and `trade-future.ir`. §א24 has the one-URL test that settles it, and it needs a phone in Iran on mobile data | — **elimination, with every other branch measured** |
| 129 | The risk the chain showed that nobody was looking for | ❌ **named, not urgent, and one line when it is measured** | `ISRG Root YE` sits between `YE2` and `X2`, cross-signed so today's devices reach a root they trust. **The pins are relying on that cross-signature.** The day a trust store ships `Root YE` as an anchor, a device may build `leaf → YE2 → Root YE` and stop, with neither pinned root in the verified chain — leaving the leaf pin alone, one `reuse_key = False` from being wrong. Bounded by the 2027-03-01 expiry either way, and fixed by pinning `Root YE` too **once somebody measures its digest**. Not written until then: a pin this repository invented is the one fault with no remote fix | — **the third certificate in the chain, still unmeasured** |

---

## א19 — the network was fine too, and the fault was in the code the whole time (5.0.5)

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 130 | The last explanation outside this app, eliminated | ✅ **measured** | `tradeyar.trade-future.ir/api/mobile/v1/auth/methods` opened in a phone browser **on an Iranian mobile network**: `200` with the real body, 442 symbols. So the host is reachable from where the reviewer was. Server up, certificate valid, pins matching, route open — every branch that put the fault outside this app is now closed by a measurement rather than by argument | — **one URL, one phone, in the right country** |
| 131 | Which sent the search back to the code, where it had been all along | ✅ **fixed** | `ErrorKind.UNKNOWN` — the catch-all for a `Throwable` that is neither a status nor an `IOException` — still read as «پاسخی نرسید». **The third instance of the same false sentence this run fixed twice**, and the only one that survives on a device whose network is fine, because it was never about the network: a body that will not parse, a field a server stopped sending, an NPE out of a mapping. The answer arrived and this side dropped it | — **`UNREADABLE`, its own reason and its own sentence in two languages** |
| 132 | And the owner's own payload named a way for it to happen | ✅ **fixed** | The measured body reports `"telegram": true` with **no `telegram_bot_username`**. Gson builds a class with no no-arg constructor through `Unsafe` — fields written directly, **Kotlin's null checks never run** — so `AuthConfigDto(val botUsername: String)` held a null in a type the compiler had guaranteed could not, and the first dereference threw frames away from the parse. Every field a server may omit is now nullable with a default | — **`AuthDtoNullabilityTest`, four cases against the live shape** |
| 133 | The second fault, found by the test failing | ✅ **fixed** | `AuthConfigDto` had no `@SerializedName`, so under `LOWER_CASE_WITH_UNDERSCORES` it asked for `bot_username` alone — while CoinePro-FX answers `botUsername`, camelCase, in the same object where it spells other keys with underscores. A camelCase key under a snake_case policy does not fail; it parses as the default. **`AuthMethodsDto` had carried both spellings for exactly this reason since 4.x, and this DTO had been missed** | — **a red test on its first run** |
| 134 | What is still not proven | ❌ **one install settles it** | That this is what the reviewer hit. It is a fault producing exactly their sentence on a device whose network is demonstrably fine, which is more than anything else left standing — and it is not proof. 5.0.5 on a phone in Iran is: the three causes are now three different sentences, and whichever appears names the branch | — **an honest gap, kept open** |

---

## א20 — the root that was holding everything up, pinned (5.0.6)

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 135 | Row 129's missing digest, measured and shipped | ✅ | `sCkq5UWXjg+7mKu9lMhhYF5bGLsy7VI/UNW3tccdR7w=` — `CN = Root YE`, issued by `ISRG Root X2`, valid **2026-05-13 → 2032-09-02**. In `DEFAULT_CERTIFICATE_PINS` and `network_security_config.xml`, both hosts, eleven digests across the two. It was never written until it was measured: a pin this repository invented is the one fault with no remote cure | — **the owner's `openssl` against the chain's third certificate** |
| 136 | What it closes, exactly | ✅ | The set had come to depend on **one cross-signature**. `Root YE` is signed by X2 today, so every device reaches a pinned anchor; the day an Android trust store carries Root YE as an anchor of its own, a device may build `leaf ← YE2 ← Root YE` and stop, with neither X1 nor X2 in the verified chain — leaving only the leaf pin, one `reuse_key = False` from wrong | — **a failure that was four years away and one line to prevent** |
| 137 | And why a root pin is durable where a leaf pin is not | ✅ **the owner's point, and it is the right one** | A pin is a digest of a **public key**, not of a certificate file. So this keeps matching if the same root is ever served self-signed instead of cross-signed by X2 — the key is one object either way — and it does not move on a sixty-day renewal. That is the whole difference between this pin and `RO8XwxTQ…`, which survives only while their certbot keeps `reuse_key = True` | — **the distinction the pin format is built on** |
| 138 | Both hosts, not one | ✅ | `coineprofx.com` already carries ISRG X1 and X2 «for the day Cloudflare moves it there». Root YE is now part of that hierarchy, so leaving it off that host would have been carrying two thirds of a reason | — **five pins on one host, six on the other** |

---

## א21 — the web's next phase, briefed from a measurement rather than from the plan

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 139 | Where the server actually is, probed rather than remembered | ✅ | `/`, `/reset` and the three legal pages `200`; `/api/app/latest`, `/api/crypto/prices` (`source: lbank-ws`, not stale), `/api/fx/prices` (19 rows, gold first), both candle routes, `/api/news`, `/api/health` (both upstreams reachable) all `200`. **Every other `/api/auth/*` is `404`.** So Phases 0–3 are done and healthy and Phase 4 is untouched | — **fifteen probes against the live host** |
| 140 | And the one route that is live is the one the plan says to delete | ✅ **measured, not assumed** | `POST /api/auth/password/reset` answers — still the **proxy to TradeYar**. An invalid token comes back as their own body: `TYR-017`, `api.tradeyar.io/errors/…`, a `trace_id`, `field: reset_token`. The undo in `SERVER_BUILD_PROMPT.md` Phase 4 has not been done | — **one deliberately-invalid POST** |
| 141 | The gate that was missing from both documents | ❌ **named before it could bite** | «Undo first. Remove the proxy» is right and its ordering is dangerous: **if TradeYar's recovery mail links to `pro-chart.com/reset`, removing the proxy locks live readers out of their own password recovery** — their token is checked against a table it was never in — while the page, the link and the form all look like they are working. `APP_LINKS.md` records `MOBILE_RESET_DEEP_LINK_BASE` as unset by default, which **was true when written and is not a measurement of today**. The brief makes checking it the precondition | — **a failure mode with no visible symptom** |
| 142 | The mail dependency, bounded to two steps instead of the phase | ✅ | `ACCOUNT.md` names a sending domain as the owner's to supply, and the older brief left that reading as though Phase 4 waits on it. It does not: **five of seven steps need no mail** — the four tables, register/login/logout/refresh, `GET /api/me`, link-on-demand, and the sync documents. Register works with `email_verified_at` left null, which is exactly what §4 already calls a Pro Chart account: a chart account, not a subscription | — **the phase reordered so the owner is the last blocker, not the first** |
| 143 | And what the store rejection taught, carried across to the server | ✅ | Cafe Bazaar refused the app because «پاسخی نرسید» stood for four different causes. The brief turns that into a rule for whoever answers these routes: a status a client can act on, a body it can parse, a `5xx` only when the fault is genuinely yours, never a `200` carrying an error — because **a field you stop sending is now a sentence on the reader's screen** pointing at whoever changed the shape | — **`ACCOUNT_BUILD_PROMPT.md` §4** |

---

## א22 — the handover failed, and the review that came back was right about the gap

| # | Item | State | Evidence | Frame |
|---|---|---|---|---|
| 144 | Nothing was built, and the cause was mine | ✅ **named** | The brief's first instruction is «Read `ACCOUNT.md` first» — and `ACCOUNT.md` lives in *this* repository, which the server does not have. One file was handed over where five were needed. `PHASE4_HANDOVER.md` is the bundle: the brief, `ACCOUNT.md` whole, and `SERVER.md` §4.4, §4.4.1, §4.5, §6 and §8, verbatim, so nothing sends the reader anywhere | — **a brief that cited a document its reader could not open** |
| 145 | «Does `ACCOUNT.md` supersede §8.1?» — settled by dates, not by assertion | ✅ | `ACCOUNT.md` is commit `ef7dcc9`, **2026-09-22**. The §8.1 answer naming TradeYar is `88189fd`, **2026-09-21**. §8.1 was rewritten in the same commit as `ACCOUNT.md` and now reads «Answered twice… **neither — the account is Pro Chart's own**». So the brief is not the stale document and the TradeYar fallback is not the answer | — **two commit dates and the rewritten §8.1** |
| 146 | And the doubt was earned — there **was** a two-reversal-stale sentence | ✅ **fixed** | `SERVER.md`'s older `/reset` subsection still read «auth proxies to **CoinePro-FX** until §8.1 is answered», written before §8.1 was answered at all, and still saying the page «does not exist on the server yet» — which it has since 2026-09-20. It now opens with a note naming all three changes and what survived them: the reasoning, not the destination | — **the likeliest single cause of «which document is current?»** |
| 147 | Two citation errors in the request, worth catching before they read as gaps | ✅ | `ACCOUNT.md` **has no §6** — it ends at §5, and the rate-limit buckets asked for are **`SERVER.md`** §6. «§4.5's three sync documents» and «§4.4.1» are `SERVER.md` too. A citation pointing at the wrong document is how a reader concludes a section is missing from a document that never had it | — **five sections, not six** |
| 148 | The one real gap the review found | ✅ **answered without waiting** | The **per-account backoff curve** is specified nowhere. `ACCOUNT.md` §3 rule 2 gives only the constraint: 10/min per address, a per-account backoff on top, and **lock nothing permanently**. The handover asks for a proposed curve in the report rather than a pause — doubling from a small base, capped at minutes, cleared by a success — and names what is outside the constraint: any permanent lock, and any counter keyed on the address alone | — **a constraint without a number, now with a proposal the owner can correct** |
| 149 | And the question about where a rule's history was written | ✅ | «The `localStorage` rule predates this reversal» traces to `ACCOUNT.md` §2's last paragraph, which says it «was already in `SERVER.md` §4.4 for the relayed design and survives the reversal unchanged». No hidden document — its author simply had both files open, which is the thing the handover had failed to reproduce | — **one paragraph, cited** |
