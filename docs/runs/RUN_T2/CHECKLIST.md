# RUN Τ2 — checklist

Four phases. Every line carries a state, the evidence behind it, and a frame — or, where a still
cannot carry the claim, the test that can and why a picture would be a lie.

States: ✅ done · ❌ not done, or narrowed (the row says exactly how) · ⏳ owed to the owner's device.

---

## The correction this run opens with

Phase A is written on a premise that is not true of this tree, and saying so is the first thing the
run owes the owner rather than something to bury in a report.

> «Phase Τ of RUN ΤΦΥ was not executed: in 4.92 the classes `ChartFling`, `ChartFrameRate` and
> `ChartStrokePredictor` … are byte-identical to 4.89.»

They are not. `2465e19` — **4.90.0 «run Τ: the frame the fling stood still on»** — is on `main`
between 4.89 and 4.91, and `git diff d3c0481..HEAD -- chart/ benchmark/` is six files: `ChartFling`
(+47), `KineticScroll` in `ChartPixels` (+13), their two test files, `ChartFlingBenchmark` (+43) and
a new `ChartFlingRegressionTest` (+60). The friction went 3.8 → **1.25** and the cut-off 20 →
**240 px/s** in that commit, and `docs/runs/RUN_TFY/CHECKLIST.md` carries the same seven rows with
the same measurements. What 4.92's APK contains is that build.

So this phase is an **audit against A1–A7 with the numbers re-measured on the current tree**, plus
the two things A asks for that run Τ genuinely did not leave behind. Nothing below is a re-statement
of a row without a fresh run behind it.

---

## PHASE A — the chart scroll

| Item | State | Evidence | Frame |
|---|---|---|---|
| **A1** Raw `VelocityTracker`, 100 ms, px/s, no density anywhere | ✅ | `CoineProChart.kt:1785` feeds the tracker from `awaitPointerEvent(PointerEventPass.Final)` — every `change.historical` sample and then the frame's own, so a 240 Hz digitiser is not resampled at the display's rate — and `tracker.calculateVelocity().x` goes **straight** into `kinetic.start` with nothing between the two calls. Compose's tracker fits over its own 100 ms horizon. `ChartStrokePredictor` is reachable only from the freehand drawing handler and no predicted event reaches the fling. Re-measured on this tree today: `ChartFlingRegressionTest` injects 3 000 px/s and recovers **2 907 px/s — 3.1 % low**, against the ±10 % A1 allows; a divide by this phone's 2.625 would read back 1 143 | — **a speed, not a still.** The test prints the figure it recovered |
| **A2** `exponentialDecay(0.9f, 150f)` | ❌ **narrowed on the two constants; met on the behaviour they are there for** | The shipped curve is `exponentialDecay(frictionMultiplier = 1.25 / 4.2, absVelocityThreshold = 240f)`. **A2's own test refutes A2's own numbers**: 150 px/s is 1.25 px a frame at 120 Hz, so a fling that runs to 150 ends *below* the 2 px/frame the same line forbids — coasting from 240 down to 150 takes 0.38 s, **45 frames** of exactly the creep the owner filmed. 240 px/s **is** two pixels a frame, which is what makes the tail impossible rather than merely shorter. And `0.9` friction is 3.78/s, which puts a 4 300 px/s release at 1 138 px — one screen — against the 2 900 px the owner measured TradingView covering. The row stays ❌ because it is not the literal ask | — **a curve, not a still.** `ChartFlingTest.it stops rather than creeping a pixel a frame` asserts a tail ≤ 3 frames and measures **0** |
| **A3** 1:1 float pan, fling starts in the hand-off frame | ✅ | The clock is seeded `HANDOFF_NANOS` (one 120 Hz frame) *before* the frame that first ticks it, in `ChartFling` and in its `KineticScroll` twin, because the finger lifts *between* frames and reading the curve at `t = 0` there spends the fastest frame of the gesture standing still | — **the absence of a frame.** `ChartFlingTest.the first frame after the release moves`: hand-off step > 0, one frame's worth (< 60 px at 3 000 px/s, measured ≈ 25), and no speed-up across it |
| **A4** hard flick ≥ 1.5 screens · 800 px/s ≥ 0.4 screens · slow drag never flings · **gutter flick does not fling time** · stop ≤ 1.4 s | ❌ **four of five. The 1.4 s cannot hold beside the distances — arithmetic below** | Re-measured today on the owner's density: hard flick **2 134 px = 1.98 screens** (≥ 1 618 asked), settled in **~2.0 s**. 800 px/s: ≥ 0.4 screens, the bound written as four tenths *less the one bar this measurement cannot see*. Slow drag at 120 px/s: ≤ 1 bar after the lift, which is the settle spring and not momentum. **The gutter clause had no test in run Τ and now does** — `a flick in the price gutter stretches the scale and never throws the time axis` drives a 3 000 px/s swipe down the price ladder at `x = 0.97 · width` and requires the window to stay put | — **gestures, not stills.** Six tests in `ChartFlingRegressionTest`, five in `ChartFlingTest` |
| **A5** 120 Hz kept; rubber-band, focal pinch, auto-scale springs untouched | ✅ | Nothing in this run touches `ChartFrameRate`, `releaseEdge`, `PinchZone` or the settle springs. Phase A's diff is three files and two of them are tests | — **an absence of change.** The commit is the evidence |
| **A6** Pencil between Indicators and the timeframe chip · portrait + landscape · Simple, Trader, Pro · Simple removes nothing | ✅ | `ChartToolbarTest` composes all six and reads the **laid-out tree**: the band's four (indicators, more, undo, fullscreen) all present, the drawing tools reachable exactly once (band pencil, or the permanent column that replaces it on a wide window, never neither and never both), and the pencil's centre between the timeframe chip's and the indicators'. Stated as an order rather than a pixel, because the band lays out right-to-left in Persian and left-to-right in English and «between» is the same sentence in both | `tau-toolbar-{simple,trader,pro}-{portrait,landscape}-fa.png` — six compositions, six frames |
| **A7** `flickVelocity` slow/medium/hard, listed in DEVICE_PROOFS | ✅ **written**, ⏳ **owed a device for its numbers** | `ChartFlingBenchmark.flickVelocitySlow/Medium/Hard`: the same finger travel over 40, 10 and 3 samples — roughly 900, 3 500 and 12 000 px/s at release on a 1 080-wide panel. Three scenarios and not one loop, because a hard release coasts two seconds across thousands of bars and a slow one settles in a few frames; one average describes neither. `docs/qa/DEVICE_PROOFS.md §1b` carries the command | — **a benchmark needs a GPU.** This container has none |

### Why A2's and A4's numbers cannot all be true

A flick on an exponential decay covers `(v − c) / f` and lasts `ln(v / c) / f`. Both are the same
two knobs pulling opposite ways, so the brief's set is a system of inequalities, and on the owner's
own phone — 411 dp at 420 dpi, **1 079 px** wide — it has no solution.

From the 3 000 px/s clause alone, at the cut-off that kills the creep (`c = 240`):

* stop within 1.4 s → `f ≥ ln(12.5) / 1.4` = **1.804**
* travel ≥ 1.5 screens → `f ≤ 2 760 / 1 618` = **1.705**

1.804 > 1.705: **empty**. Raising the cut-off to 300 px/s opens a window at `f ≈ 1.65`, and closes a
different clause — an 800 px/s release then covers 303 px, 0.28 of a screen against the 0.4 asked
for, and a 4 300 px/s one covers 2 424 px against TradingView's measured 2 900. Making the friction
depend on the release speed does not rescue it either: **both** failing constraints are evaluated at
the same 3 000 px/s, so no `f(v)` can satisfy them.

What is shipped keeps the distances — which is what «کند» was — and pays for them in the one
second the brief wanted back. TradingView takes two seconds itself on the owner's own recording.
`BLOCKED.md §A` states the choice the owner owns.

---

## PHASE B — the gaps the 4.92 audit found

| Item | State | Evidence | Frame |
|---|---|---|---|
| **B1** Monogram fallback everywhere; a symbol is never hidden for a missing logo | ✅ **already shipped, 4.91.0** | Run Φ replaced `SymbolArtwork.covers` with the monogram: `CoineProAssetLogo` draws the disc under every logo — «the monogram is not a placeholder that gets swapped for a spinner: it *is* the logo until the picture arrives» — and `CoineProColors.monogramHue` is the stable hash of the ticker, with saturation and lightness fixed so every disc in a list carries the same weight. `SymbolUniverse`: «a market with no artwork is drawn as a monogram and **is still listed**». Tests: `SymbolListingTest`, `MonogramTintTest` | Run Φ's own set. Not re-taken: this run changed no code behind B1, and a fresh frame of unchanged pixels is evidence of nothing |
| **B2** `entitlements.all` as a real switch, **server-fed**, local default `true` | ✅ | The switch and both its states were already there (`Entitlements`, `EntitlementGateTest`); what was missing is the word **server-fed**, which the old KDoc admitted — «server-fed in intent and local in fact». Now: `EntitlementsGateway` GETs one field on the crypto deployment, `EntitlementStore` keeps it, `EntitlementStartUp` applies the stored value at process start and refreshes for the next launch. **Applied at start and never mid-session**, deliberately: a wall that appears while a reader is standing in the doorway reads as the app breaking. Every failure — no route, no signal, a body that will not parse — answers null, and null keeps the app open | — **a flag, so not a still.** `EntitlementGateTest` now has seven cases, two of them new: a silence leaves the app open, and a served refusal reaches every derived answer |
| **B3** `forexTrading = false` airtight | ❌ **narrowed on one key of the four named** | The gap this found and closed: **`copy-trade` survived the flag**. It is keyed to the forex *platform*, which is a different question from whether this build trades there at all, so a reader on CoinePro-FX still had the row. Both call sites now drop it with `connections`. `ForexSurfaceReachabilityTest` reads the shell's own hiding rule out of `CoineProApp.kt` rather than retyping the ids, drives both states, and asserts the hidden set stays inside the trading block. **What is not done:** `membership_open_ourbit` is deliberately kept — the membership journey is the **crypto** venue's sub-account check, and `FeatureFlags.forexTrading`'s own note keeps «the account verification the crypto venue requires» either way. Removing it would take crypto sign-up out of a crypto app. The row stays ❌ because the line names that key | — **an absence, so not a still.** Four cases in `ForexSurfaceReachabilityTest`, plus `TradePartnersFlagTest` for the chart's partner row |
| **B4** Five tabs; everything of 4.92 still at depth ≤ 2 | ✅ **already shipped, 4.92.0** | `AppDestination` is دیده‌بان · چارت · رَصد · انجمن · منو, and `NavigationDepthTest` holds every declared route to the menu, the bar, or a named exemption | `upsilon-markets-tabs-phone-fa.png` and the rest of run Υ's set, unchanged: this run did not touch the bar or the tabs, so re-taking them would be a picture of the same pixels |
| **B5** Pulse figures only; the explanation behind ⓘ | ✅ | The row printed figures already; what it did not have is the **affordance** — a tappable cell that looks like a printed one is a door nobody opens. A 12 dp ⓘ now sits beside each cell's name, and the *target* stays the whole cell, because a 12 dp mark is below every minimum a thumb is measured against. The four `_why` sentences are unchanged, word for word | `t2-b5-pulse-row-phone-fa-dark.png`, `t2-b5-pulse-sheet-phone-fa-dark.png`. The test also pins what the picture cannot: that none of the four explanations is drawn *on* the row |
| **B6** Offline is a first-class state | ✅ **built, and the second half turned out to be a different feature than the row described** | **«ذخیره‌شده · ۲ ساعت پیش», on the chart, the watchlist, news and the calendar.** `CoineProOfflineBar` answers «is the phone online» and clears when the reader walks to a window; it says nothing about how old anything is, and a phone that dropped its network ten seconds ago draws the same bar as one that has been in a basement since yesterday. `CoineProSavedBar` is the other sentence, and it is true on a perfect connection too — a screen restored from the cache while a request is in flight is just as old. **Drawn in the muted ink, not a warning colour**: every screen in this app is meant to be readable from the cache, and painting that fact amber would say the thing the app was designed to do has gone wrong. **The age ticks** — a bar that read «۲ دقیقه پیش» when the page opened and still said it forty minutes later would be the lie the bar exists to remove, and the reader could not tell. Each surface reports the age of **what it is actually showing**: news and the calendar per section, because the controller holds a section whose refresh came back empty and the two halves are then of different ages; the chart from the cache's own write time rather than the newest bar's, because a daily candle is legitimately twenty hours old on a live chart. And the chart's failure branch already said «the banner is what says they are old» about a banner that did not exist — it does now. **The second half is not a queue.** A local alert is not sent anywhere: it is stored on the phone and `LocalAlertWorker` compares it against prices under a **connected** constraint. So an alert armed on a plane is not waiting to be uploaded — it has never once been read against a market, while the screen says «فعال». That is the run's recurring failure shape, and the pill says «هنوز بررسی نشده» rather than inventing a flush for a queue that does not exist. **And a defect nobody had reported**: the diagnostics page's cache-age field was `it.toString()` on raw epoch millis, so it had been printing `1758592800000` where a sentence belongs since the field was added | — **31 cases across four files.** 11 in `SavedAgeTest`, of which two are the file's reason to exist: an unknown or future timestamp produces **no age at all** rather than «just now» — a phone whose clock moved is not looking at data from the future, and clamping to zero would print «همین حالا» over a picture that might be days old — and every unit floors rather than rounds, so fifty-nine minutes is «۵۹ دقیقه» and a picture can never be made to sound fresher than it is. 5 in `MarketIntelHeldTest` for the per-section hold, including the fetch that threw leaving everything held at its own age. 8 in `AlertReachTest`: every alert is unchecked when no pass has ever succeeded (the fresh install with no network, which is the reader who most needs telling), a paused alert is never reported because that is the reader's own decision, and a row from before creation times were recorded counts as checked rather than putting a pill on every old alert after an update. 2 more in `AlertEvaluatorTest`, and they are the pair that matters: a pass that could not read prices does **not** stamp, because stamping it would clear the pill on exactly the passes that checked nothing |
| **B7** Two Glance widgets | ❌ **not done in this session** | One widget exists (`MarketsWidget`, RemoteViews, with its configuration activity and a WorkManager refresh). The single-symbol widget and the Glance rewrite are not started. `RESUME.md` carries it | — |
| **B8** Picture-in-Picture | ❌ **not done in this session** | Nothing exists. `RESUME.md` carries it | — |
| **B9** Alert sound and repeat | ❌ **narrowed on the bundled sound set; the rest is done** | **«repeat every N minutes until acknowledged»** is new and is the half none of the three old policies could express: `AlertRepeat.UNTIL_ACKNOWLEDGED`, an interval on the alert, an `acknowledged()` that ends it, a «دیدم» button on the notification and an acknowledgement when the reader opens it. Offered in both editors — the composer's chip row and the editor sheet's own row, where it sits *beside* the bar frequency rather than inside it, because bars and the clock are different units and a reader can want both. **Do Not Disturb**: the loud channel plays on the alarm output, which is the one sound a «priority only» filter lets through, so it is capped back to the ordinary channel while the phone is in DND — and `setBypassDnd` appears nowhere in this app. Vibration per alert and the four separate channels were already there. **What is not done:** the *bundled* sound set — this repository has no audio assets and `BLOCKED.md §B9` carries the contract for them | `t2-b9-until-seen-phone-fa-dark.png`, plus six cases in `AlertAcknowledgementTest` for the parts a still cannot carry — the interval, the end, the clamp, and that acknowledging any other policy is a no-op |
| **B10** One sentence about what this app is not | ✅ | «این اپ معامله انجام نمی‌دهد و کارمزدی دریافت نمی‌کند.» / "This app does not trade and takes no commission." — drawn directly **under the identity block** on the menu, not in the footer: the foot of a nine-section directory is a place a reader arrives at by accident, and «prominent» and «at the bottom» are not the same instruction. Same sentence in `docs/product/STORE_LISTING.md`, and the test compares the two strings so they cannot drift | `t2-b10-menu-phone-fa-dark.png`, `t2-b10-menu-phone-en-light.png` |

## PHASE C — six features no competitor has

| Item | State | Evidence | Frame |
|---|---|---|---|
| **C5** Compare on the chart | ❌ **narrowed on two clauses; the legend figure is new** | Already shipped: `Comparison.kt` normalises to PERCENT — rebased to the **left edge of the viewport**, so the comparison re-answers itself as the reader pans, which is the whole point — and to INDEXED_100, RATIO and ABSOLUTE; the hub's «مقایسه با نماد دیگر» tile adds from the watchlist; the legend carries a removable pill per series. **New in this run:** `changeAcrossVisible` and the figure beside each symbol — the move **across the bars on screen**, anchored on the first bar that traded and read to the last one that did, «—» rather than «۰٪» where there is nothing to measure. **Narrowed, twice.** (1) «A one-tap swap between normalised % and separate scales»: every basis is already one tap, but *separate scales* — each series on its own price axis — is a feature this chart does not have, and ABSOLUTE is not it (raw values on one axis put a $2 300 instrument and a $0.42 one on the same ladder, which is the flat line the enum's own note warns about). (2) «Reachable from the watchlist's «تحلیل»»: that action opens the **side-by-side panes** run Υ built, which is a different comparison — several symbols, each with its own chart — and pointing it at the overlay instead would take the multi-chart away | — **a figure that changes as the chart is panned, so not a still.** Four cases in `ComparisonTest`: the visible window rather than the series, a blank window reporting nothing, the anchor skipping to the first bar that traded, and a window past the end being clamped rather than thrown |
| **C1** «Why this move?» | ✅ **the rule reached the glass, and the sheet is bound to the same arithmetic** | `NotableBars` decides which bars — range greater than twice the average range of the **preceding** twenty, the bar kept out of its own baseline, the first twenty never marked, a flat window answering nothing, and a window with gaps averaged over the bars that traded. **New in this run: the surface.** A small dot under each such bar, in the **event strip** and not in the plot — a tap on the plot already means a drawing, an eraser stroke or a trade ring, and taking one of those over would put the app's most delicate gesture path at risk. Where an event glyph already holds a bar, the dot is not drawn and the glyph takes the touch: the glyph names something that happened and the dot only says the bar was large. The sheet says how big the bar was against that chart's own recent normal, how far it moved open to close, and **what was on the calendar and the wire inside that bar's own half-open window** — unfiltered by the reader's glyph switches, because they asked about one bar. Where nothing fell inside, it says exactly that in a sentence rather than putting the nearest headline under «چرا»: markets move for reasons nobody publishes, and naming one would be manufacturing a cause. The figure the sheet prints comes from `NotableBars.ratioAt`, beside the rule that placed the dot and never re-derived — **a mark on a bar whose sheet says nothing unusual happened is worse than no mark at all** | — **the arithmetic, asserted against itself.** 15 cases in `NotableBarReadingTest`, the first of which is that the dot and the sheet agree on the same series; 5 in `NotableStripHitTest` (nearest rather than first, and a bar an event glyph already holds is not offered); the original 7 in `NotableBarsTest` unchanged |
| **C2** Alerts on drawn objects | ✅ **built, and the audit found a silent failure and a second one beside it** | The gap was not the two surfaces, it was what they were hiding. `GuestAlertMarketSource` says in its own KDoc that «a drawing the reader deleted … is left out entirely and its alert simply never fires» — correct evaluation and a **silent death**: the alert sat under «فعال» reading as armed and could never go off, and the reader found out by not being told. Three things now. (1) **The chart asks before it deletes a line that carries alerts** — three answers, because *keep everything*, *take both* and *take the line and keep the alerts* are three intentions and the third is what somebody about to redraw the line wants. Only where alerts exist: run Ω2's «one tap, no question» stands untouched for a line nothing watches, which is the whole of the trade it made. (2) **The alert centre marks an alert whose drawing is gone**, in the refusal colour rather than «متوقف»'s grey, because it is not the reader's decision. (3) **A sketch of the line rides on the row**, drawn from the drawing's own stored anchors — `feature:alerts` still does not depend on `core:chart` and `AlertDrawingSketch` says why in its own KDoc. And the audit turned up a **second defect nobody had reported**: the toast's «واگرد» after deleting a drawing had never worked. `deleteDrawing` wrote the new layer straight into the state, bypassing `onDrawing` — the one place a drawing step was recorded — so the button either did nothing or took back an unrelated change. It records now, once per deletion, so a selection of five is one step | — **not a still: the failure was an alert that looked alive.** 12 cases in `DrawingAlertDeleteTest` (including the undo, which failed before the fix, and the negative one: a drawing nothing watches still goes on the tap), 7 in `AlertOrphanRowTest` (including «nothing is marked while the drawings have not been read»), 10 in `AlertDrawingLinksTest` |
| **C3** Duel with the past | ✅ **built, and the rule that makes it worth playing is the one that refuses to score** | One round a day: a moment out of the reader's **own** markets — `Duel.roundFor` picks from the catalogue they watch, not from a quiz bank — with 120 bars of context behind it and the next 20 hidden. One question, up or down, and the moment they answer, those 20 bars appear. It is a **mode of the chart and not a screen**, for `ArenaSession`'s reason and more strongly: the reader has to be reading the chart they trade on, and the hidden bars are hidden by `ReplayState.visible`, which is the mechanism the replay mode already uses — there is no path by which the answer is on screen before the call. **A move under `Duel.FLAT_PERCENT` is `TOO_CLOSE` — neither right nor wrong.** Without that rule every round resolves, half of them on noise, and a reader converges on fifty percent while learning that a coin flip is a read; with it the record counts only the rounds the market actually answered and says how many it set aside. **No timer**, because the Arena's five minutes rehearse trading under pressure and reading a chart is not that. **No second call**: once the outcome is set the buttons are gone and `DuelSession.answer` ignores a second one, because a reader who could call again with the next twenty bars in front of them would be recording a prediction they did not make. **One round a day, and the refusal is said out loud**: `DuelStore.answer` reads and writes the day inside a single `edit` and returns whether it recorded — a record that could be padded by tapping «same chart, five times» is not a record, and a refusal the screen swallowed would look exactly like a counter that stopped working. **And no hit rate under `Duel.MINIMUM_ROUNDS`**, on `MyWeek`'s argument and with `MyWeek`'s shape: `DuelRecord.rightPercent` is null there, the band has no branch that invents one, and it prints the count instead | — **32 cases across three files, and two of them are the point.** 18 in `DuelTest`: that a barely-moved market makes the reader neither right nor wrong, that the resolving bar is always inside the series the caller loaded (asserted across every series length where the window only just fits), that the same day gives the same round and a different day does not, that an unanswerable round is not judged at all rather than counted as a loss, and that the duel and the Arena do not pick the same instrument — different LCG constants, so the app reads as having two ideas rather than one. 8 in `DuelStoreTest`, of which the once-a-day guard and its **reported** refusal are the file's reason to exist; also that a phone whose clock went backwards is not locked out for ever. 6 in `DuelSessionTest` for the second-call refusal and for a refused round still showing its verdict — the record refused it, the market did not. One thing the tests could not assert and it is written into `DuelTest` instead: the exact `FLAT_PERCENT` boundary is unreachable in binary floating point (a close of 100.5 against 100.0 comes back as 0.4999999999999858), so both sides are asserted and the unreachable middle is written down rather than faked |
| **C4** My week | ✅ **built, and the number it refuses to print is the feature** | A card at the top of the journal: trades closed this week, how many won, alerts that fired against alerts still armed, replay sessions — over **the reader's own week**, which opens on Saturday in Iran and across the Gulf. `WeekStart` moved out of `core:marketdata` into `core:common` for it: the weekly candle and the reader's week must open on the same day, and a second copy of a boundary is the copy that gets corrected alone. **Under five closed trades the win rate is withheld** — not rounded, not greyed, not hedged — and the count is shown instead. Two of four is fifty percent and means nothing; one outcome moves the figure by twenty-five points, which is what makes a small-sample percentage a lie told in a confident font. The card has no fallback, so the only way to break the rule would be to add one. An untouched week is a sentence rather than four zeros, and armed alerts — a standing state — do not make one look busy. **And one row is deliberately not wired**: the only change figure this app holds for a watchlist is the feed's own *24-hour* one, so «moved most this week» would have been a lie in the same font. `WeekSummary.moverSymbol` stays null until something measures a week, and the card omits the row | — **14 cases in `MyWeekTest`**, of which two are the file's reason to exist: that the week opens on Saturday for a reader in Tehran and Monday for one in London, and that four trades produce **no percentage at all**. Also the half-open boundary at both ends, and a quiet week having no manufactured mover |
| **C6** Rasad's morning brief as a notification | ✅ **built, and the hard part was deciding what it refuses to say** | Once a day, at an hour the reader picks, one notification: how their watchlist stands, which market moved most, what the chart says about it, and a line of that market's night. **Off until they switch it on** — every other category here reacts to an event the reader already asked about, and this one arrives on a clock; an app that starts waking somebody daily without being asked is the one they uninstall. **It is sent on a quiet night too**, and that is the decision rather than an oversight: a scheduled brief that sometimes does not arrive is indistinguishable from a broken one, and the reader's answer to that is to stop relying on it. What it will **not** send is a brief about nothing — an empty list, or a pass where no change could be read at all, produces silence rather than a notification with a blank in it, and a market whose change the feed omitted is left out of the count rather than counted as flat. **One-time work chained to itself, not `PeriodicWorkRequest`**: a period takes an interval and not a time of day, so «every 24 hours from 07:00» drifts a little later every run and cannot follow a reader across a time zone. The sparkline is a closing line with no axes and no labels, coloured **first against last** so a night that fell and bounced on the final bar is still drawn as a fall — which is what the sentence above it says | — **three pure pieces, each testable without waiting until tomorrow.** 12 cases in `RasadBriefTest` (the Latin figure beside the Persian count, the quiet floor, the tie going to the reader's own order), 12 in `MorningBriefScheduleTest` (the boundary going forward and never to zero, the double-wake guard), 9 in `MorningBriefEngineTest` (the four refusals, the forming bar left out), 10 in `BriefSparklineShapeTest`. That last file is the one worth reading: the first version tested the **bitmap**, which needed Robolectric's native graphics — the legacy shadows rasterise nothing, so the colour assertion passed against an empty image — and native graphics then loads a library *outside* the JVM heap that took the `:app` suite past the container and had it killed with SIGKILL, exit 137, no failing test anywhere. So the geometry came out into `BriefSparklineShape` and what is left in the painter has nothing to decide |
