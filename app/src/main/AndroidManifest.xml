<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".CoineProApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoinePro"
        android:usesCleartextTraffic="false">

        <profileable android:shell="true" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                tools:node="remove" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
                Hosts are named one by one on purpose. A bare scheme claims every coinepro:// URI
                in existence, which is more than the app can act on, and Android does not verify
                ownership of a custom scheme at all — any other installed app may register the
                same one. These two are what CoineProFirebaseMessagingService actually emits, and
                nothing that carries a credential is ever routed this way.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="coinepro" android:host="signal" />
                <data android:scheme="coinepro" android:host="activity" />
            </intent-filter>

            <!--
                Password recovery arrives as an https link rather than a coinepro:// one, because
                it carries a reset token. An App Link is verified by the system against
                /.well-known/assetlinks.json on the domain, so no other app can intercept it;
                a custom scheme offers no such guarantee.

                Until the Play signing certificate exists, assetlinks.json cannot be published and
                verification fails — which is harmless. The link opens in the browser and the web
                page completes the reset, exactly as it must anyway for someone who opened the
                email on a desktop. Publishing assetlinks.json later turns the same link into an
                in-app one with no change here and no change to the email.

                CoinePro-FX's own domain joins this list once it is known.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="trade-future.ir"
                    android:pathPrefix="/app/" />
            </intent-filter>
        </activity>

        <service
            android:name=".notifications.CoineProFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>
